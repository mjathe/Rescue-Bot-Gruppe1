driveToPerson
{
if(findObstacle(person) == true)
{
  engine.transmission(0)
  rescue()
}
else
{
  AudioNavigation();
  if(findObstacle(branch)== true)
  {
    engine.transmission(0)
    callOperator
    activateChainSaw()
  }
  else if (ultrasonic == true)
  {
  engine.transmission(0);
  }
  else
  {
    engine.transmission(1)
  }
}
}
