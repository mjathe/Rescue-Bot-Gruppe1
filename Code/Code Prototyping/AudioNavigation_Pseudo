 bool [4] audioData // data from audio sensors
int alignmentToDrive // calculated angle from audio singnals


AudioNavigation( ) // wenn fahrzeug audio erhält und noch nicht in reichweite ist ausrichten auf schall und dahin fahren
{
  bool[] audioData = readAudioSensors() // wenn person erreicht rettung
  if ( personInRange() == true)
  {
      rettung !
  }
  else
    {
        alignmentToDrive = Alignment_Pseudo.driveAlignment(audioData)
        engine(alignmentToDrive,direction)
    }
}
